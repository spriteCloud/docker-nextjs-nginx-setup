# spriteCloud Gitlab setup

## Installation
- have a linux distribution
- install docker-ce
- install docker-compose

## Running the containers
Start with the command
```
docker-compose up -d
```
Stop with the command
```
docker-compose down
```
